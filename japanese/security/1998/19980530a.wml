#use wml::debian::translation-check translation="db1de9b4d182abe0614b8e9fa5648020926d480e"
<define-tag description>klock にバッファオーバフロー、kvt
が設定を root で保存</define-tag>
<define-tag moreinfo>
<p>klock が動作しているシステムで単純なバッファオーバフローを悪用して
グループシャドウにアクセス可能になるという報告を受け取っています。また、
kvt にも一般ユーザではなく root で設定を保存するという問題があります。</p>

<p>直ちに kdebase パッケージをアップグレードすることを勧めます。</p>
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980530a.data'

#use wml::debian::translation-check translation="a344c983d62e0f902caa8d31afb51481f4b569ad"
<define-tag description>バッファオーバフローによりローカル
root アクセスを許可</define-tag>
<define-tag moreinfo>
<p>lprm にあるバッファオーバフローがユーザにローカルシステムへの root
アクセス権限を与える可能性があるという報告を受け取っています。</p>

<p>hamm かそれ以降にリリースされたバイナリの使用を勧めます。</p>
</define-tag>

# do not modify the following line
#include '$(ENGLISHDIR)/security/1998/19980828a.data'
